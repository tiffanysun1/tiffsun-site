---
import Layout from '../layouts/Layout.astro';

const blogPosts = [
  {
    title: "Benchmarking Pull Request Code Reviews",
    slug: "benchmarking-pull-request",
    date: "August 3, 2025",
    excerpt: "I built a lightweight benchmark to test 5 major AI models on real pull request decisions from major open-source projects like Kubernetes and VS Code. Most models turned out to be \"Yes Men\" - approving 80-90% of PRs including problematic ones."
  }
];
---

<Layout title="Blog - Tiffany Sun" description="Tiffany Sun's blog posts about software engineering and AI">
  <!-- Keep the tiffany sun link at the top -->
  <div class="intro-text" style="position: fixed; top: 1.5rem; left: 2rem; z-index: 20; transform: none; font-size: 2rem;">
    <a href="/" style="text-decoration: none; color: inherit;">tiffany sun</a>
  </div>

  <div class="blog-index-layout">
    <main class="blog-index-content">
      <h1>Blog</h1>
      
      <div class="blog-posts">
        {blogPosts.map(post => (
          <article class="blog-post-preview">
            <h2><a href={`/blog/${post.slug}`}>{post.title}</a></h2>
            <div class="blog-date">{post.date}</div>
            <p class="blog-excerpt">{post.excerpt}</p>
            <a href={`/blog/${post.slug}`} class="read-more">Read more →</a>
          </article>
        ))}
      </div>
    </main>
  </div>
</Layout>

<style>
  .blog-index-layout {
    max-width: 800px;
    margin: 0 auto;
    padding: 6rem 2rem 2rem 2rem;
  }

  .blog-index-content h1 {
    font-size: 3rem;
    margin-bottom: 1rem;
    font-weight: 600;
  }

  .blog-description {
    font-size: 1.1rem;
    color: #666;
    margin-bottom: 3rem;
    font-style: italic;
  }

  .blog-posts {
    display: flex;
    flex-direction: column;
    gap: 3rem;
  }

  .blog-post-preview {
    border-bottom: 1px solid #eee;
    padding-bottom: 2rem;
  }

  .blog-post-preview:last-child {
    border-bottom: none;
    padding-bottom: 0;
  }

  .blog-post-preview h2 {
    font-size: 1.8rem;
    margin-bottom: 0.5rem;
    line-height: 1.3;
  }

  .blog-post-preview h2 a {
    text-decoration: none;
    color: inherit;
    transition: color 0.2s;
  }

  .blog-post-preview h2 a:hover {
    color: #666;
  }

  .blog-date {
    color: #666;
    font-size: 0.9rem;
    margin-bottom: 1rem;
    font-style: italic;
  }

  .blog-excerpt {
    line-height: 1.6;
    margin-bottom: 1rem;
    color: #333;
  }

  .read-more {
    color: #666;
    text-decoration: none;
    font-weight: 500;
    transition: color 0.2s;
  }

  .read-more:hover {
    color: #333;
  }

  @media (max-width: 768px) {
    .blog-index-layout {
      padding: 5rem 1rem 2rem 1rem;
    }

    .blog-index-content h1 {
      font-size: 2.5rem;
    }

    .blog-post-preview h2 {
      font-size: 1.5rem;
    }
  }
</style>